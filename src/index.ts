/**
 * PALEE - Personal Adaptive Learning Environment Engine
 * Phase 1: Core deterministic implementation
 */

import pkg from '../package.json';
export const version: string = pkg.version;
export * from './types';
